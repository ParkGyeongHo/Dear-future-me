<!DOCTYPE html>
<html>
    <head>
        <% include ./templates/materialize_head.ejs %>
    </head>
    <body>

    <% include ./templates/materialize_body.ejs %>
    <% include ./templates/header.ejs %>
    <% if(user){ %>
    <h4 class="center">마이페이지 수정</h4>
    <div class="row">
        <form class="col s12" action="/user/<%=user.userid%>" method="post">
            <div class="row">
                <div class="input-field col s6">
                    <input disabled id="user_id" type="text" name="userid" value="<%= user.userid %>">
                    <label for="user_id" class="active">유저 아이디</label>
                </div>
                <div class="input-field col s6">
                    <input id="user_name" type="text" name="username" class="validate" value="<%= user.username%>">
                    <label for="user_name" class="active">유저 이름</label>
                </div>
            </div>
            <div class="row">
                <div class="input-field col s12">
                    <input value="<%= user.email %>" name="email" id="user_email" type="text" class="validate">
                    <label for="user_email" class="active">이메일</label>
                </div>
            </div>
            <div class="row">
                <div class="input-field col s12">
                    <input id="birthday" type="text" name="birthday" class="validate" value="<%= user.birthday %>">
                    <label for="birthday" class="active">생년월일</label>
                </div>
            </div>

            <h6 class="center">가입 날짜 : <%= user.joinDate %></h6>
            <div class="center" style="margin-top: 40px">
            <button id="modify" class="btn waves-effect waves-light" type="submit" name="action">수정
            </button>
            </div>
        </form>
    </div>
    <%}else{%>
        로그인 후 이용해 주세요.
    <%}%>
    </body>
</html>